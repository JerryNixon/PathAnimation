<Page
    x:Class="SampleProject.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SampleProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:c="using:CustomControls.Controls"
    mc:Ignorable="d">

    <Page.Resources>
        <Storyboard x:Name="MyAnimationStoryboard">
            <DoubleAnimation From="0" To="1" Storyboard.TargetName="MyPathAnimationWrapper" Storyboard.TargetProperty="Progress" />
        </Storyboard> 
    </Page.Resources>
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <c:PathAnimationWrapper x:Name="MyPathAnimationWrapper" Stretch="None">
            <TextBlock Text="Sample" />
        </c:PathAnimationWrapper>
    </Grid>
    
</Page>
